import type { HabitIconPack } from '../types'

/** Sugar — natural sweetness motif. Fruit, bloom, fresh energy. */
export const sugarIconPack: HabitIconPack = {
  greeting:      '🍓',
  encouragement: '🌸',
  success:       '✨',
  restart:       '🌱',
  resetHero:     '🫐',
  achievements:  '🏅',
}
